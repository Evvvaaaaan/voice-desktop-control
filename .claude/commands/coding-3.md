---
description: Dispatch a self-contained coding task to the coding-3 parallel worker subagent
argument-hint: <task description>
---

Use the Agent tool with subagent_type "coding-3" to handle this task. Write the agent's
prompt to be fully self-contained — the subagent has no memory of this conversation, so
include relevant file paths, what to change, and what to verify before reporting done.

Task: $ARGUMENTS
